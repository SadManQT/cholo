# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Same convention as server/ — .env.example is committed, .env is not
# (nothing here is actually secret, VITE_* vars ship to the bundle either
# way, but one gitignore rule for "local env overrides" beats two conventions).
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
