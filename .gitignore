# secrets — never commit
.env
.env.local

# dependencies
node_modules/

# build output
dist/
build/

# coverage
coverage/

# OS cruft
.DS_Store

# machine-specific agent config (hardcoded to this contributor's home dir)
.codex/

# playwright MCP session artifacts (ephemeral, per-run)
.playwright-mcp/
